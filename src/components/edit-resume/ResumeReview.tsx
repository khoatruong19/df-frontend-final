import React from 'react';
import Stockholm from '../resume-templates/Stockholm';

type Props = {};

const ResumeReview = (props: Props) => {
  return (
    <section className="w-[50%]">
      <Stockholm />
    </section>
  );
};

export default ResumeReview;
